{% extends 'base.html.twig' %}

{% block title %}Hello TaskController!{% endblock %}

{% block body %}
<div class="container">
    <div class="jumbotron">
        <ul>
            {% for single_task in task %}
            <li>{{single_task.name}}</li>
            {% endfor %}
        </ul>
    </div>
</div>

</div>
    {{form(formulaireTask)}}
</div>
{% endblock %}
